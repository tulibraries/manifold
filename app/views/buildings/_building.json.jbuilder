json.extract! building, :id, :name, :description, :address1, :temple_building_code, :directions_map, :hours, :phone_number, :image, :campus, :email, :created_at, :updated_at
json.url building_url(building, format: :json)
