<% unless field.nil? %>
	<%= link_to t("manifold.default.fields.springshare_id.appointment_link.label"), 
				t("manifold.default.fields.springshare_id.appointment_link.link", id: "#{field.to_s}") %>
<% else %>
	<%= field.to_s %>
<% end %>